[project]
name = "seb-markers"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "opencv-contrib-python>=4.8",
    "numpy>=1.26",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["corner_tracker"]
